import Display from './CalcDisplay'
import { useState } from 'react';
function HomePage(){
    const [theme, toggleTheme] = useState("day");
    
    let 
    return <div><Display/></div>
}
export default HomePage;